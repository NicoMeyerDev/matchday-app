from django.apps import AppConfig


class FormationsConfig(AppConfig):
    """Registriert die Formationen-App im Django-Projekt."""

    default_auto_field = 'django.db.models.BigAutoField'
    name = 'formations'
